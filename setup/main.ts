import { defineAppSetup } from '@slidev/types'

// Import glitchcore theme styles
import '../styles/glitchcore.css'
import '../styles/transitions.css'

export default defineAppSetup(({ app }) => {
  // Any additional Vue app setup can go here
})
